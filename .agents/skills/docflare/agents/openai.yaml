interface:
  display_name: "Docflare"
  short_description: "Repo-specific patterns and workflows for docflare"
  default_prompt: "Use the docflare repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true